apiVersion: v1
kind: Service
metadata:
  name: camel-country-summary
spec:
  selector:
    app: camel-country-summary
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
